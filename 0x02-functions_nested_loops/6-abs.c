#include "main.h"

/**
 * _abs - display the absolute value of a number
 *
 * @n: the int input
 *
 * Return: the absolute value of the number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
