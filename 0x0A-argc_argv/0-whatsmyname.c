#include <stdio.h>

/**
 * main - prints the program name
 * @argc: argument count
 * @argv: arument vector
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;

	return (0);
}
